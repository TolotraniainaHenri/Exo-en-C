#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "point_mil.h"
int main() // Methode du point mulieu
{
    int n;
    double x, y;
    f(x);
    subdivision(&n);
    y = calcul(n);
    valeur(y);

    return 0;
}