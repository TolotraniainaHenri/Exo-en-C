#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "simpson1.h"
int main() // Methode de Simpson
{
    int n;
    double x, y;
    f(x);
    subdivision(&n);
    y = calcul(n);
    valeur(y);

    return 0;
}