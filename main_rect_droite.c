#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "rect_droite.h"
int main() // Methode du Rectangle droite
{
    int n;
    double x, y;
    f(x);
    subdivision(&n);
    y = calcul(n);
    valeur(y);

    return 0;
}