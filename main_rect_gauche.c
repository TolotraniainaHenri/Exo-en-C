#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "rect_gauche.h"
int main() // Methode du Rectangle gauche
{
    int n;
    double x, y;
    f(x);
    subdivision(&n);
    y = calcul(n);
    valeur(y);

    return 0;
}